<h1>New track</h1>

<% if @track.errors.any? %>
  <h3>Oops there were some errors. Please try again:</h3>
  <ul>
    <% @track.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_for @track do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit  %>
<% end %>
